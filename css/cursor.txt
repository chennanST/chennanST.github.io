/* CSS代码 */
body {
  cursor: url('/cursor/default.png'), default; /* 默认鼠标样式 */
}

a {
  cursor: url('/cursor/pointer.png'), pointer; /* 链接鼠标样式 */
}

textarea,
input[type='text'],
input[type='button'],
input[type='submit'],
input[type='search'] {
  cursor: url('/cursor/text.png'), text; /* 文本输入鼠标样式 */
}

.not-allowed {
  cursor: url('/cursor/not-allowed.png'), not-allowed; /* 不允许操作鼠标样式 */
}

.zoom-out {
  cursor: url('/cursor/zoom-out.png'), zoom-out; /* 缩小鼠标样式 */
}

.zoom-in {
  cursor: url('/cursor/zoom-in.png'), zoom-in; /* 放大鼠标样式 */
}

.grab {
  cursor: url('/cursor/grab.png'), grab; /* 抓取鼠标样式 */
}
